const productId=new URL(window.location.href);teddyId=productId.searchParams.get("id"),console.log(teddyId);const productApiUrl="api/teddies/"+teddyId;console.log(productApiUrl),fetch(productApiUrl).then(data=>data.json()).then(product=>{const mainContainer=document.getElementById("myData");mainContainer.innerHTML="voici les couleurs: "+product.colors+" </br>Et la description ici: "+product.description});